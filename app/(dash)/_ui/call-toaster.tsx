"use client";

/**
 * Placeholder. Workspace C replaces this file — see prompts/round4/C-toasts.md.
 * Contract: a client component mounted once in the dashboard layout that polls
 * /api/dash/calls?since=<ISO> and raises one sonner toast per new call, with a
 * distinct persistent style for escalations. Renders nothing itself.
 */
export function CallToaster() {
  return null;
}
